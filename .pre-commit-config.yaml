repos:
  - repo: local
    hooks:
      # ── Auto-update README task counts ──
      - id: readme
        name: Update README
        entry: npm run readme
        language: system
        files: ^README\.md$
        pass_filenames: false
