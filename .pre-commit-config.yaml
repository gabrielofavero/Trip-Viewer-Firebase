repos:
  - repo: local
    hooks:
      # ── Auto-update README task counts ──
      - id: readme
        name: Update README
        entry: bash -c "python3 scripts/utils/readme.py"
        language: system
        files: ^README\.md$
        pass_filenames: false
