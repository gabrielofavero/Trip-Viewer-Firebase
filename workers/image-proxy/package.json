{
	"name": "trip-viewer-image-proxy",
	"version": "1.0.0",
	"private": true,
	"description": "Cloudflare Worker that batch-downloads trip images for TripViewer's Export as Static Web Page (complete mode). One POST with ALL image URLs → every image returned in a single response, fixing the 0-byte CORS-blocked download bug.",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev --port 8789 --inspector-port 9231",
		"deploy": "wrangler deploy"
	},
	"dependencies": {
		"firebase-auth-cloudflare-workers": "2.0.6",
		"trip-viewer-firebase": "file:../.."
	},
	"devDependencies": {
		"wrangler": "^4.120.0"
	}
}
