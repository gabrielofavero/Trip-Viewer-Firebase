{
	"name": "trip-viewer-places-api",
	"version": "1.0.0",
	"private": true,
	"description": "Cloudflare Worker proxying Google Places API (New) for TripViewer (local / dev / prd from one codebase).",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy"
	},
	"dependencies": {
		"firebase-auth-cloudflare-workers": "2.0.6"
	},
	"devDependencies": {
		"wrangler": "^3.0.0"
	}
}
