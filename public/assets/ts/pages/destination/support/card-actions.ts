// ======= Destination Card Actions (P4) =======
// P2 stub — renders nothing. P4 replaces this with the clickable link-button
// row (website / map / instagram / video).

export function getCardActionsHTML(item) {
	return '';
}
