// ======= Destination Card Edit (P5) =======
// P2 stub — renders nothing. P5 replaces this with the owner-only edit button
// (never in the action row).

export function getCardEditHTML(item, j) {
	return '';
}
